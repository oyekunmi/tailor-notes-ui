addons:
  chrome: stable

language: node_js
node_js:
  - "8"

branches:
  only:
    - master
    - feature/travis

before_script:
  - npm install -g @angular/cli
  - npm install

script:
  - ng build --prod --aot